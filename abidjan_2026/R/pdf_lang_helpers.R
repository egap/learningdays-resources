# Bilingual-only stubs for plot helpers (PDF exports keep EN + FR).

deck_lang <- function() {
  "both"
}

pick_lang <- function(en, fr) {
  en
}

pick_lang_pair <- function(en, fr) {
  list(en = en, fr = fr)
}
